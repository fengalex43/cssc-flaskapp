apiVersion: v1  
kind: Service  
metadata:  
  name: {{ include "mychart.name" . }}  
spec:  
  type: LoadBalancer 
  ports:  
    - port: 80  
      targetPort: 80  
      protocol: TCP  
      name: http  
  selector:  
    app: {{ include "mychart.name" . }}  